language: php

php:
  - 7.0
  - 7.1
  - nightly
matrix:
    allow_failures:
        - php: nightly
        - php: 7.1
before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script: phpunit
